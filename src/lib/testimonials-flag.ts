/**
 * Testimonial sections across the site are built and styled, but every
 * quote in them is still placeholder text. Flip this to true once real
 * client testimonials are ready to go in — every section wrapped with
 * SHOW_TESTIMONIALS will come back at once.
 */
export const SHOW_TESTIMONIALS = false;
